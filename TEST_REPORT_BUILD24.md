# Build 24 Test Report

`xcodebuild build` was attempted with the iOS 26.5 simulator SDK. The project parses after Build 24 registration, but dependency resolution could not complete because this environment cannot resolve `github.com` for RevenueCat's Swift package. No test count is claimed as a result.
